<li class="list-group-item burger-item">
    {{burger_name}}
    <button class="btn devour-btn" data-id="{{id}}" data-devour="{{devour}}">
        {{#if devour}}EAT IT!{{else}}DIGEST IT!{{/if}}
    </button>
</li>